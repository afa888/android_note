package com.example.login;

import android.util.Log;

import com.example.arouterlibrary.Arout.Arouter;
import com.example.arouterlibrary.Arout.IArouter;
import com.example.login.LoginActivity;

/*
public class ActivityUtils implements IArouter {
    private static final String TAG = "ActivityUtils";

    @Override
    public void putActivity() {
        Log.e(TAG, "putActivity: ");
        Arouter.getInstance().putActivity("login/login", com.example.login.LoginActivity.class);
    }
}
*/
